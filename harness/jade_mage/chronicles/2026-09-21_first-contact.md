# 2026-09-21 · first contact

*Chronicle of the jade_mage lane. What happened on this side, in the order it happened. Nothing here
is a claim about the upstream work beyond what its own files record.*

## What was read

The public repository at `35c65bd` (2026-09-20), 35 commits, plus the dated records in `research/`
back to 2026-09-11. Three agent runtimes appear in its files in sequence: Codex for the kickoff and the
orientation, Antigravity for the gates that were later falsified, and a runtime signing commits as
Claude Fable 5.1 for the last two days. The discipline lived in the pasted protocol and the `AGENTS.md`
files and survived both switches.

## What was found

- The owner ran `agentprivacy.org/discovery.md` (v0.3) verbatim on 2026-09-12 and reported every one of
  our eight areas as "documented only". That is correct, and it is the first complete external execution
  of the survey.
- They found a live defect in our skill catalog: the `dual-agent-harness` packet v1.0 names a superseded
  checkout as its canonical home. Verified still live on 2026-09-21. They refused to follow it.
- They kept the fence on our Z/(2^6)Z algebra: `neg(bnot(x)) = succ(x)` is not a balanced-ternary law.
- Their 2026-09-18 held-out audit dissolved 23 self-graded gates (validation split 93 to 99 percent
  verbatim in training) and kept them on the record with status banners. A pre-registered rule then
  passed by its letter (0.032 nats against 0.03; rho 0.027) and was filed as a fail of its intent; the
  thesis was retired.

## What was done

1. Read-only review from a scratch clone. No file of theirs changed.
2. A memory note on our side recording the adoption and the packet defect.
3. Fork created at `mitchuski/ternary-memory-research`, cloned with `upstream` set. `main` tracks theirs.
4. Lane directory opened locally; instance markers scaffolded with the harness's `new_instance.mjs` and
   left unfilled, so `conform.mjs` refuses them honestly.
5. The overlap note written: 16 sections, share-safe (no local paths, no private hosts, no em-dashes).
6. The first contribution written in their conventions: `experiments/verify_carrier_symmetry.py`,
   27 exhaustive checks; `research/carrier-symmetry.md`; ledger `outputs/carrier-symmetry.json`.
   Run once from the project root: `{"status": "passed", "check_count": 27}`.
7. Branch `agentprivacy/overlap-note-2026-09-21` on the fork: the note under `sources/agentprivacy/`
   and the three contribution files. Pushed. Pull request opened upstream.
8. Branch `harness/jade_mage` on the fork: this directory. Pushed. Never proposed upstream.

## Later the same day: the first follow-up

The keeper asked for thoughts on neuromorphic applications and then for a follow-up into the pull
request "if there is progress to be made". Progress was made rather than argued:

- checker section 11, the differential conductance pair of their deep dive 09: swapping the two lines is
  the only write-free map and it induces exactly `N_0`; the two zero-moving swaps are the cheapest
  reprograms (2 cell writes over the three codes) but can never be free, and each moves the non-conducting
  code off the zero weight; the write-free set equals the product-pushable set. 33 checks, all exhaustive.
- `research/carrier-symmetry.md` §3b states the reading and its fence (a write is a state change of an
  idealised binary cell; no device physics is claimed).
- the overlap note's pathway P10, "ternary on metal": the 16 September additive-GEMM verdict as a
  substrate mismatch; what maps to an array and what does not; two tests without hardware (an aihwkit
  phase-change drift curve on arm C's tiles; the exit price λ in energy per tile pass).
- second commit `fb709e6` on the PR branch; comment posted on PR #1. The owner had not replied at that time.

## What waits on the owner

The catalogue entry (by invitation), the acknowledgement paragraph (needs a name), any census record,
and the remaining offered pull requests (PR-2 to PR-5 of the note's §9). The packet fix on our side
is owed regardless.

## The lesson carried

A stranger read the door before opening it, and the door held. The middle phase of their work is what a
self-grading runtime produces without a hold-apart; the last phase is what the hold-apart restores. Both
are in one public record with the ledgers for each. That is the harness's central claim, demonstrated by
someone with no reason to demonstrate it for us.

(⚔️⊥⿻⊥🧙)😊
